# class Node:
#   def __init__(self, val):
#     self.val = val
#     self.next = None

def get_node_value(head, index):
  
  loop = 0
  
  while loop <= index:
    
    if head == None:
      return None
    
    value = head.val

    loop = loop + 1
    head = head.next

  return value
